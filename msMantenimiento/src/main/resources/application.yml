server:
  port: 8086

spring:
  application:
    name: msMantenimiento
  datasource:
    url: jdbc:mysql://localhost:3306/db_mantenimiento
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    enabled: true
    version: openapi_3_0
  swagger-ui:
    enabled: true